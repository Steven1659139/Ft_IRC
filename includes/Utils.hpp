// #ifndef UTILS_HPP
// #define UTILS_HPP

// #include <string>
// #include <vector>

// namespace Utils {

//     // Divise une chaîne de caractères en un vecteur de chaînes basé sur un délimiteur
//     std::vector<std::string> split(const std::string& s, char delimiter);

//     // Supprime les espaces de début et de fin d'une chaîne de caractères
//     std::string trim(const std::string& s);

//     // Convertit une chaîne de caractères en minuscules
//     std::string toLower(const std::string& s);

//     // Convertit une chaîne de caractères en majuscules
//     std::string toUpper(const std::string& s);

//     // Vérifie si une chaîne correspond à un nombre entier
//     bool isInteger(const std::string& s);

//     // Encode une chaîne de caractères pour éviter les injections ou pour la transmission sur le réseau
//     std::string encode(const std::string& s);

//     // Décode une chaîne de caractères reçue du réseau ou d'une source externe
//     std::string decode(const std::string& s);

// }

// #endif // UTILS_HPP
